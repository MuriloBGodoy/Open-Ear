# Copie para .env.local e preencha. .env.local não é versionado.
#
# Chave da Groq: https://console.groq.com/keys
# Em produção isto NÃO vive em arquivo — vive nas Environment Variables do
# site no Netlify. Aqui é só para `netlify dev`.
GROQ_API_KEY=gsk_...
